# 🚀 Автоматический запуск Backend

## ✅ Работает без изменения Jenkins Pipeline!

Backend будет **автоматически запускаться** при каждом деплое через **systemd path watcher**.

## Как это работает

1. **Backend файлы автоматически копируются** в `dist/` при сборке (через brojs)
2. **Jenkins загружает файлы** на сервер в `main/`
3. **При первом запуске** `setup-backend.sh` устанавливает:
   - Systemd path watcher (отслеживает изменения файлов)
   - Systemd timer (проверяет каждые 5 минут)
4. **При каждом деплое** path watcher автоматически запускает backend

## Что нужно сделать

### Первый деплой: Один раз запустить скрипт

После первого деплоя нужно **один раз** запустить на сервере (если есть доступ):

```bash
cd /usr/share/nginx/html/apps/TravelFrog/main
chmod +x scripts/setup-backend.sh
./scripts/setup-backend.sh
```

**Если нет доступа к серверу** - попросите администратора выполнить эту команду один раз.

### Следующие деплои: Всё автоматически!

После первого запуска backend будет запускаться автоматически при каждом деплое.

## Настройка Nginx (один раз)

Попросите администратора добавить в конфигурацию Nginx:

```nginx
location /api/ {
    proxy_pass http://localhost:3000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
}
```

## Итого

✅ **Не нужно менять Jenkins pipeline**  
✅ **Backend файлы копируются автоматически** при сборке  
✅ **Backend запускается автоматически** при каждом деплое  
✅ **Всё работает из коробки!**

Подробности: `AUTO_START_BACKEND.md`

